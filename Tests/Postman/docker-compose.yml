version: '3.4'

services:
  postman:
    image: web-scanner-postman
    container_name: web-scanner-postman
    build:
      context: .
      dockerfile: Dockerfile
      args:
        ENVIRONMENT: local
      